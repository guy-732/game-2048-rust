export component MainWindow inherits Window {
    Rectangle {
        Text {
            text: "hello world";
            color: green;
        }

        background: black;
        width: parent.width;
        height: parent.height;
    }

    min-height: 100px;
    min-width: 200px;
}